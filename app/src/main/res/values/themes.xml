<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChessClocks" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryDark">@color/main_background_color</item>
        <item name="statusBarBackground">@color/main_background_color</item>
        <item name="android:windowLightStatusBar">@color/main_background_color</item>
    </style>

    <style name="Theme.ChessClocks" parent="Base.Theme.ChessClocks" />
</resources>